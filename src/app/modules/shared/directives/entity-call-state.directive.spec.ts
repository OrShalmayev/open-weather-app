import { EntityCallStateDirective } from './entity-call-state.directive';

describe('EntityCallStateDirective', () => {
  it('should create an instance', () => {
    const directive = new EntityCallStateDirective();
    expect(directive).toBeTruthy();
  });
});
